/// <reference types="node" />
import tls from 'node:tls';
import { IpAddressTypes } from './ip-addresses';
import { AuthTypes } from './auth-types';
export declare interface ConnectionOptions {
    ipType: IpAddressTypes;
    authType: AuthTypes;
    instanceConnectionName: string;
}
interface StreamFunction {
    (): tls.TLSSocket;
}
export declare interface DriverOptions {
    ssl: boolean;
    stream: StreamFunction;
}
export declare class Connector {
    private readonly instances;
    private readonly sqlAdminFetcher;
    constructor();
    getOptions({ ipType: rawIpType, authType: rawAuthType, instanceConnectionName, }: ConnectionOptions): Promise<DriverOptions>;
    close(): void;
}
export {};
//# sourceMappingURL=connector.d.ts.map